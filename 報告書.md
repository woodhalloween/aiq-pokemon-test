TypeScript型システムとランタイム動作の検証報告書
1. 調査概要
目的
PokemonSpecies型がなくても実行時にエラーにならない原因の特定
実際のPokemon APIから返されるデータ構造の確認
TypeScriptのコンパイル時とランタイム時の挙動の違いの検証
調査方法
テスト用コードの作成と実行
APIレスポンス構造の分析
ローカル環境とDocker環境での挙動の比較
2. 問題の特定
src/utils/api.tsファイルでPokemonSpecies型をインポートして使用していましたが、src/types/pokemon.tsに定義されていませんでした。
Apply to test-pokemon...
;
しかし、ローカル開発環境ではエラーが表示されず、アプリケーションは正常に動作していました。
3. 調査結果
3.1 型消去のメカニズム
TypeScriptの型情報はコンパイル時にのみ存在し、JavaScriptにコンパイルされると完全に消去されます。実行時には型情報は存在しません。
Apply to test-pokemon...
;
3.2 APIレスポンスの実際の構造
テストコードで実際のAPIレスポンスを検証したところ、以下の構造を持つことが確認できました：
Pokemon API:
id, name, sprites（多数の画像URL）、types（タイプ情報）など
PokemonSpecies API:
id, name
names配列（各言語の名前情報）
日本語名は language.name === 'ja' で取得可能
color, genera（分類）など多数のメタデータ
3.3 コンパイルとランタイムの挙動の違い
コンパイル時：型チェックが行われ、型が存在しない場合はエラー
ランタイム時：型情報は消去され、オブジェクトの実際の構造のみが重要
オプショナルチェイン (?.) などを使用することで、型がなくても安全にプロパティにアクセス可能
3.4 ローカル環境でエラーが表示されない理由
package.jsonのscriptsセクションで開発サーバーの起動にnext dev --turbopackを使用していることが原因：
Apply to test-pokemon...
}
Turbopackは開発体験を高速化するために型チェックを分離し、即時のエラー表示を抑制します。
3.5 Docker環境でのエラー発生
Docker環境では次の理由でエラーが検出されます：
Dockerfileでnpm run buildを使用しており、完全な型チェックが実行される
Docker環境はクリーンな状態で構築され、ローカルの.nextキャッシュがない
コンテナ内では標準のTypeScript設定が適用され、型チェックが厳格に行われる
4. 解決策
4.1 適切な型定義の追加
src/types/pokemon.tsにPokemonSpecies型を追加：
Apply to test-pokemon...
}
4.2 型チェックの確認方法
エラーを検出するには：
npx tsc --noEmitを実行
VSCodeなどのエディタで直接確認
npm run buildを実行してビルド時のエラーを確認
5. 結論
TypeScriptの型システムは開発時の安全性を提供しますが、実行時には型情報が消去されるため、型定義がなくても実行時エラーは発生しません。適切な型定義を追加することで、開発時のエラー検出やコード補完機能を活用でき、コードの品質と保守性が向上します。
開発サーバーの設定（Turbopack）により型チェックが遅延される場合がありますが、これは開発体験の高速化のための設計です。本番環境や継続的インテグレーションでは完全な型チェックが行われるようにすることが重要です。
